<%= form_for Review.new do |f| %>
  <% if Review.new.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(Review.new.errors.count, "error") %> prohibited this review from being saved:</h2>

      <ul>
      <% Review.new.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
